language: python
python:
  - "3.6"

branches:
  except:
    - master

before_install:
  - ./gitclone.sh

install:
  - pip install requests
  - pip install datetime
  - pip install lxml
  - pip install pytz

before_script:
  - python ./check.py

script:
  - python ./build.py

after_success:
  - ./gitpush.sh

notifications:
  email:
    on_success: always
    on_failure: always
