language: python
python:
  - "3.6"

branches:
  except:
    - master

before_install:
  - ./gitclone.sh

install:
  - pip3 install requests
  - pip3 install datetime
  - pip3 install lxml
  - pip3 install pytz

before_script:
  - ./check.py

script:
  - ./build.py

after_success:
  - ./gitpush.sh